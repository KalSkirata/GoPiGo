<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ziri.gopigo_new">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Gopigo_menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConnexionRaspberry"></activity>
        <activity android:name=".GopigoActivity"></activity>
        <!-- <activity android:name=".BddData"></activity> -->
        <activity android:name=".GraphActivity"></activity>
        <!-- <activity android:name=".SynchroniseBdd"></activity> -->


    </application>

</manifest>